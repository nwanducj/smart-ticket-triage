/**
 * useAuth Hook Re-export
 *
 * Re-exports the useAuth hook from the AuthProvider for a cleaner import
 * path: `import { useAuth } from "@/hooks/useAuth"` instead of importing
 * from the provider file directly.
 */

export { useAuth } from "@/providers/AuthProvider"
